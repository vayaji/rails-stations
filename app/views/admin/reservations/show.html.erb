<div class="max-w-xl mx-auto mt-10 p-8 bg-white rounded-lg shadow-md">
  <%= render 'form', reservation: @reservation %>
  <div class="mt-6">
    <%= button_to "Delete Reservation", admin_reservation_path(@reservation), method: :delete, class: "bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-4 rounded transition" %>
  </div>
</div>